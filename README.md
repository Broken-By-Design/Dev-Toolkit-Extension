# KAC Toolkit
an extension for developers.

## Build instructions
Run `npm install` to install dependencies  
Next run `npm run build`.  
After that the built code will be in `./dist/`
OR  
run `bash build.sh` on any Linux machine with npm/node.js installed on it
